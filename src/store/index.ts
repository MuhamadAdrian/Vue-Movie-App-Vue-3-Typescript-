import { createStore } from 'vuex'
import { bookmark } from './bookmark'
export default createStore({
  state: {
  },
  getters: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    bookmark: bookmark
  }
})
